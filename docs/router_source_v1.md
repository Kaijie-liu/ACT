# Source-level router coverage, not another lower-bound search

User asks to continue toward strict complete MoE certificates. The preceding
gate/candidate study is CLOSED negative and is not reopened. The distinct
question here is whether a named existing complete request can replace its
trusted numerical route exclusions with independently checked source-parameter
proofs. No new neural verification problem, optimizer, sample or time extension.

## Target and scope

Only the old convolutional input98 proof: four-expert output selected-softmax
top2,67.06%-accuracy checkpoint epoch89,2/255 represented input box, sole declared
pair[1,2],9 checked positive output obligations. Exactly its pinned checkpoint,
materialized tensor bytes and original portable proof. The0/30 production SAFE
and0/20 evidence study remain unchanged. This single-pair source control cannot
be called route-changing or high-accuracy.

The original portable proof still assumes network/input->HZ, guards, and route
exclusions. This extension addresses ONLY route coverage. Capture records the
raw binary64 router/input bytes and all34 state-tensor identities. A stdlib
checker recomputes the same aggregate model hash and materialized input hashes.
It checks the declared AvgPool(nonoverlap,no padding)/Flatten/Linear graph
dimensions and semantics. No point probes are substituted for symbolic bounds.

For each score difference, the producer computes disjoint pool intervals. The
independent checker distributes the coefficient difference back over ORIGINAL
pixels and exactly evaluates its box minimum/maximum with Fraction arithmetic.
For any candidate pair S, if some outsider j strictly dominates an insider i
on the whole box, S is impossible. A zero lower bound never excludes a tie.
All binomial(E,2) routes must be present, excluded with checked strict evidence
or conservatively covered. This rule is sound but not a complete feasibility
method when different guard inequalities jointly cause infeasibility.

The combined checker must freshly recheck all9 old output obligations and show
that its independently derived route cover discharges EVERY old exclusion.
If the new cover is larger, or identities/semantics disagree, stop without a
new complete claim. No new support solves or relaxed comparison tolerances.

## What remains trusted even on success

This is a theorem about the declared real graph and pinned represented box,
not the requested real epsilon-ball under arbitrary rounding. The capture
inspects the known factory router and binds its tensors, but is not an
independent proof of Python/PyTorch dispatch, checkpoint deserialization or
deployed floating kernels. Declared graph correspondence to the registered
eval program remains explicit. Expert network/input->HZ and guard lowering
remain trusted; this extension does not validate those transformations.

Do not erase that remaining graph-correspondence assumption while removing
trust in numerical route infeasibility reports. Do not claim complete strict
real-network certification merely from successful source-level routing.

## Execution and acceptance

After analytic controls, freeze sources then commit/push before one execution.
New local directory only; old bundle copied unchanged, extension files added
separately and hash-bound out of band. One300s stored-source budget covers
capture, exact proposal, packing, isolated complete check and publication;
build60s/check180s caps, work deadline298s. Original propagation/LP generation
is excluded and cannot be subtracted from previous production timings.

The moved bundle uses python-I-S with read restriction to bundle/stdlib,
no model/data/checkpoint/history/solver access during checking. Independent
postterminal review reruns the same complete check, checks all hashes/costs,
and records its separate time. Failed construction, missing evidence, cutoff
or inability to reproduce all exclusions is retained; no automatic retry.
Control tests cover all ties, multiple covers, dimension changes, corner
differential, modified operators/parameters/inputs, missing/duplicate routes,
wrong exclusion witnesses and mutated bounds. This is trust reduction, not
performance optimization; no public artifact/weights are published.
