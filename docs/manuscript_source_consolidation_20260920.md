# Manuscript consolidation after the input98 stop

Starting branch `feat/moe-route-verification`, clean at
`7a409319cb4177896c6b221ccefc2935d5ca24db`. This stage implements Advice/dd.md's
remaining manuscript-focus instruction after the bounded diagnosis, **not a
new experimental protocol**. No solver, training, model forward, source
propagation, new proof query, external contact or artifact publication occurs.

## Completed edits

The manuscript separates two non-interchangeable evidence paths:

1. The historical stored-HZ input98 proof has nine positive obligations, with
   expert lowering still assumed. Its portability does not remove that premise.
2. The new declared-source path checks represented input containment, complete
   expert transformations, factor maps, routes and output-LP construction, but
   has no complete positive output proof. Its latest property-directed arm
   retains eight nonpositive obligations and a missing seventh property.

The old positive duals cannot be attached to the new source, nor can the new
source proof validate the old enclosure retrospectively. Different source and
gate contracts also prevent treating their contrast as a causal measurement of
removing an upstream assumption. No theorem, implementation, acceptance gate,
frozen result, denominator or main-table number changes.

Soundness engineering now describes the source-checking contract by operation
and evidence boundary rather than development order. Evaluation keeps the
old conditional positive, latest complete-source negative and stop decision
visible, linking the intermediate record. Abstract, introduction, method,
discussion, reading order and reviewer workflow use the same distinction.

The original123 evaluation lines,88 soundness lines and33 discussion lines
are preserved **verbatim**, totaling244 lines, in
`paper/appendices/source_proof_history.md`. The preservation receipt binds
each original path, source commit, exact block hash and byte count. Historical
phrases such as “the next control” remain history, not instructions to resume.
All evidence files remain where they were, with the same bytes.

## Checks and scope

The companion `manuscript_source_consolidation_20260920.json` pins preserved
blocks, supporting evidence and the unchanged rendered main table. Stdlib-only
tests check exact preservation and reject removed/altered blocks, validate local
links, verify evidence hashes and distinguish historical positive assumptions
from the new negative output endpoint. Existing main-table tests revalidate
counts, grades and denominators. These are manuscript/accounting checks,
**not independent reproof of network safety or a fresh experiment**.

Completed validation: six new manuscript tests and three unchanged main-table
tests pass. Isolated `python -I -S scripts/rebuild_moe_main_tables.py --check`
confirms the same21 rendered rows. All15 pinned evidence/table files match;
the prior81 execution-file and13 captured-artifact hashes also match.

The operational handoff continues to forbid input98 retries, added rows/time,
acceptance changes and new local searches. No new experiment is queued. The
existing scoped first-family gains remain valid under their registered numerical
policy; external complementarity and convolutional/general-evidence negative
results remain in the main narrative. High-accuracy/cross-family route-changing
strict positivity remains open. This edit makes no paper-complete or
venue-acceptance declaration.

## What now needs a new decision

No further guidance was needed for this manuscript consolidation. A new
experimental direction, however, needs a separately scoped research decision:
the closed diagnosis did not identify a justified next input98 control. Do not
invent another query, model, budget or arithmetic branch merely to continue.
Submission-specific formatting needs the PI's venue choice; public empirical
bundle/weight distribution remains PI-managed. Neither can be silently treated
as already approved or completed. Existing prose/evidence can continue to be
reviewed without reopening those decisions.
