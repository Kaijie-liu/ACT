# One candidate for the sole blocked complete-request obligation

New finite decision following the user's request to continue, not an extension
of the fixed-dual study. That study remains negative26/27. Its new rational LP
was never optimized with the narrower gate; reusing old duals does not test
whether a different candidate proves the same LP positive.

## Frozen hypothesis and acceptance

Only seed1/index4018, pair[3,5], property1, exactly the stored new LP from
`checked_gate_saved_20260920_v1`. No new samples, models, supports, precision,
gate construction, difference ranges or numerical thresholds. One SciPy/HiGHS
candidate call, native cap90s. It only proposes multipliers. Exact finite-box
dual checking, rational McCormick reconstruction and ALL27 original obligations
are independently checked before any complete conditional result is accepted.

The new pure checker composes a replacement proof with the old request. The
old checker is unchanged. It proves exactly one old residual is nonpositive,
requires exactly one unknown in the fully rechecked inventory, then binds the
replacement to that row, source, property, router supports and domain. Missing
obligations, wrong pair/property, malformed supports or insufficient bounds
cannot yield completion. Certificate support bindings use canonical object
hashes in this new schema, distinct from historical file-byte hashes.

The single candidate may fail or stay nonpositive. No retries, changed options,
additional cases or larger caps follow automatically. Nonpositive exact lower
bounds do NOT prove LP impossibility or network unsafety; no feasible upper
certificate is attempted. This is not another custom elimination experiment.

## Budget and evidence

Outer300s includes startup, stored-source validation, proposal conversion and
exact checking, serialization, a fresh python-S complete-request checker,
and terminal publication. Stage caps80/120/80s, work deadline298s, single-thread
environment,8GiB address-space worker cap. Missing/cutoff phases are unavailable,
not zero-cost completed work. Logs and partial files survive owned-process
termination. No claim of survival of host reboot or supervisor kill.

Original model propagation and historical support-generation costs are excluded:
this is a supplied-source proof diagnostic, NOT a production timing experiment.
Independent postterminal review is separately timed and never added twice.
Launch only after controls, protocol/source hashes, commit and push; require
clean feature checkout and CPUload/core<=.5. Preserve output directory on failure.

Even a positive27/27 result remains first-family, postselected, conditional on
network/input->HZ, guard lowering and router exclusions. The three legal pairs
are conditional coverage inputs, not new route-flip witness searches. No
high-accuracy, convolutional, full-network or deployed-float claim is upgraded.
Production verification status and old results remain untouched.
