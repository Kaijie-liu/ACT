# Modular real-LP diagnostic V1 — freeze only

After passing modular arithmetic and supervision controls/fresh reviews, freeze
the **same four supplied original LP obligations**, in their original order:
input220_p0, input222_p1, input230_p2, input232_p0. No extra sample, property,
point-dependent selection, new model or changed LP. Source/statement hashes,
native binary/runtime, exact arithmetic, journal, worker, auditor, batch and
archive sources are bound to the new execution identity.

Output: `data/moe/results/modular_diagnostic_real_20260920_v1`, absent at freeze
and selection-review time. A distinct namespace retains all old primitive and
fidelity diagnostics. Freeze status is FROZEN_NOT_EXECUTED, not new efficacy.

## Fixed budget and permitted operation

- Original start: construction/capture/mapping cutoff218s, checker/work298s,
  publication300s; original 4096-bit and all modular/component caps unchanged.
- One native fidelity-V2 call <=10s, one basis and one modular reconstruction
  invocation. The up-to128 primes are internal bounded rounds, not 128 basis
  retries. No resume, fallback basis/arithmetic, increased time or threshold.
- One CPU worker with fixed thread policy; same resource gate and owned-process
  cutoff rules. No GPU, training, network propagation, or F0 regeneration.
- Proposed points only establish feasibility/upper bounds if the unchanged
  standalone checker validates all original LP constraints and bindings.
- LIMIT, modular exhaustion and TIMEOUT remain unresolved. ERROR stops the
  batch; every later roster row is retained NOT_RUN_AFTER_ERROR with null costs.
  Partial journals and modular candidates are never proof substitutes.

## Required records / interpretation

Preserve raw import/capture, mapping, original-system hash, modular rounds and
first-limit context, arithmetic journals, result serialization, isolated check
and terminal/publication records, including failures. Costs cover supplied-LP
startup, load, import, mapping, proposal, package/check and in-request audit.
Phase, constructor, attempt and batch levels are nested, not summed repeatedly;
resource waiting and post-terminal audits are reported separately. Historical
network-to-HZ/guard/F0 work is excluded, so no full-MoE speed claim.

Before comparing old and new arithmetic, check basis structure and exact
assembled-system identity against the saved primitive run. Finite-field pivot
choices may differ across primes and from primitive elimination; matching basis
does not imply matching pivots or matched timing. A checked nonpositive LP
upper bound concerns the supplied relaxation only, not network UNSAFE. No new
dual lower bound or optimality claim is generated by this protocol.

Batch controls must check complete/error/timeout rosters, null missing costs,
terminal and source mutations, missing records, old/source comparison, and
clean-branch/remote launch gates. Fresh review replays the saved ledgers and
relocates analytic check packages without native solves or reconstruction.
Then read-only original selection/import-size compatibility is independently
rebuilt. This does not measure real modular fill, bit growth or runtime.

## Execution gate

Only a later explicit `python -m modular_diagnostic.run launch --execute-frozen`
may execute, after the freeze and its selection review are committed/pushed on
a clean feature branch, under the shared writer lock. No launch in this turn.
Do not alter this freeze after observing results; preserve all four outcomes.
