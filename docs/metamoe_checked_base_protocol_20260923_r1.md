# Checked current-assignment base: repaired-source two-request diagnostic

After same-object BN point conformance PASS, freeze exactly two executions of
old MNIST0: repaired-source native protected baseline, then checked-base mode.
Same physical tensor, checkpoint, properties/order, 2/255, original numeric
policy, 300 s request, <=30 s expert, base <=10% of the expert allocation,
8 GiB sampled outer RSS / 2 GiB sparse representation policy. No retry,
new image, grid, deduplication, new relaxation or altered solver options.

Only checked mode changes the expert BASE feasibility proposal. It makes one
fresh zero-free-factor assignment, binds request/input/nonce/full matrix,
checks ALL rows/domains/integrality and publishes evidence in the SAME base
deadline. A failure falls back to native with remaining time (or UNKNOWN).
No cached point, guard exemption, dropped output obligation, fake native
infeasibility, or changed full-model UNSAFE replay. Point conformance is not
assumed for arbitrary assignments; this path only establishes feasibility of
the stored HZ under the existing float policy. Source completeness stays false.

Controls: full safe/unsafe native differentials; center outside guard; foreign
nonce/matrix; truncated/invalid point; expiry, late publication, exception and
partial evidence; base feasible while properties unresolved; context cleanup.
All original protected-property and assignment tests remain applicable.

Full router candidate/support/propagation/loading costs are paid independently
by both executions. Outer watchdog and terminal precedence are reused unchanged.
Tracing and all check/publication costs are charged. Evidence distinguishes
non-native checked BASE from native property queries. No free serialization.
Postflight inventory/hash costs and independent audit cost are reported apart
from charged request execution, not hidden. Fixed native-first order is NOT
a statistical timing experiment. No claim of speedup/general effect from two
requests; no comparison of outcomes to the old defective BN matrix.

Before run: tests, commit/push, freeze, commit/push, resource check. New root
only, no resume. Stop following error/source drift. Audit from stored matrices
and terminal records without new solving. Report every property, base and
property status, allocation/observed costs, same fresh matrix/projection checks.
Remaining UNKNOWN is not evidence of weak relaxation: distinguish absent
native return, completed feasible candidate with original-model replay, and
completed infeasibility under the existing numerical contract. No further
representation change is authorized by this protocol.
