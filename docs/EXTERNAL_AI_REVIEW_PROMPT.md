# 可直接转发给另一 AI 的评审指令

请作为未参与实现的、持审慎态度的研究评审者，独立审查 ACT／HybridZ
MoE 验证项目。不是续写代码，不是替作者证明结论，也不是承诺录用。
可以反对我们的主张，但每项重要判断必须给出代码／证据位置和推理。

项目目录：`/data1/Kane/MOE/ACT`。
分支：`feat/moe-route-verification`。
科学材料评审基线：`806443470b3eeda3e601510ea8a6fc71570f96bf`。
后面的交接提交只用于组织评审；先报告你实际看到的 HEAD、分支、工作区状态。
若基线或文件无法访问，明确写出限制，不假装已读或已执行。

先读 `AGENTS.md` 和 `docs/CODEX_HANDOFF.md` 的最新部分；随后按
`docs/EXTERNAL_AI_REVIEW.md` 进行评审。
`docs/external_ai_review_manifest_20260921.json` 固定了正文、代码和结果材料。
`/data1/Kane/MOE/Advice/ee.md` 是上轮建议（仅服务器可能有），不是结论的证据。
不需要旧聊天记录；文件与可核验制品优先于任何聊天总结。

研究目标：保留同一输入、router、合法 route guard 和专家输出的关系，
在路由可能变化时证明完整 weighted MoE 的输出性质，并公平检验净收益。
当前生产范围为 eval、CPU/float64、输出层 selected-softmax top-2；不要
把数学推广或实验模块当成已经支持任意深层/token-level MoE。

本轮是**只读评审**：不要改代码、配置、结果或文稿，不提交／推送，不安装
依赖，不训练、不新增求解／前向／传播，不重开 holdout 或 input98。
不要因看到建议而直接修复。报告先回复在聊天里；需要落盘或新增实验时先说明范围。
本地可使用已有 act-py312 执行说明中的只读、无求解检查。
若共享工作区不干净或分支不符，遵循 AGENTS 停止并报告，不清理别人的工作。

请特别核查（不要把这里的总结当作已证实）：

1. 主表 179／156／141 个 HZ-policy SAFE 和 23 个增益，在最新输入包含缺口
   下究竟能支持什么，哪些原有“证书／鲁棒性”表述必须收窄或撤回？
2. 理想数学归约、浮点源转换、求解器接受政策、结构审计、独立有理数检查，
   是否被真正分开？“所有合法路由”是否依赖尚未核验的来源？
3. 旧 supplied-HZ 正证明与新 source-checked 非正／缺失结果是否被错误拼接？
4. 方法是否有超出通用 HZ／McCormick／缓存／算法选择的 MoE 专属贡献？
   同预算对照、关系消融和外部路径是否真的支持对应归因？
5. 没有新的来源完整真实模型正证书时，什么范围的论文仍可成立？什么是
   必修缺口，什么不是投稿的必要条件？不要用“再多跑实验”代替具体论证。

最终交付：执行范围声明；你自己重述的问题与贡献；逐条问题清单（严重度、
文件行号／JSON key、证据、影响、最小修正建议）；主张—保证矩阵；
投稿前必修与可延后事项；最多三个有明确验收／停止条件的下一步。
区分“已确认问题”“疑点”“材料不足”。重建表格不等于重证 SAFE。
另一个 AI 的审查也不等于人类第三方专家审阅或机器检查证明。

请直接评审，不只复述我们的进展，也不要为了显得严格而把尚无证据的
可能性写成已发生的错误。无需给出录用概率或“稳拿 A”的承诺。
