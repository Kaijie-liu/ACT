# One saved-factor provenance replay, zero native queries

Saved-only assignment R2 returned a policy-valid base point in0.01859s, with
6918variables/3847constraints checked,maxresidual2.78e-17. It also produced
violations of ALL16 historically UNKNOWN output rows. This is **not** an
original-model witness. It motivates a distinct finite provenance check,not
more solver time,another free-factor seed,or a relaxation modification.

One oldMNIST0,one saved factor assignment,expert1 ONLY. Reconstruct router and
the same guarded expert HZ under unchanged model/input/config; block native
MILP entry and intercept output verification as capture-only UNKNOWN. Require
full fresh model fingerprint equal the saved matrix identity before using the
point. Check it again, recover input through the actual shared input factor
map, require domain membership, and run original full-model and padded-expert
forward on that one input. No native router/candidate/support/property queries;
no new factor proposal or search. Prior recorded complete route coverage is
used solely to select the object for this diagnostic,not certify a new request.

Total outer diagnostic limit30s including loading/validation/propagation/replay/
serialization,8GiB sampled own-group RSS,original2GiB representation admission,
CPUfloat64/two threads. This is a new source-correspondence diagnostic,NOT an
extension of prior property solve budgets or a performance/full-verifier rerun.
No retries,other expert,alternate point or automatic cohort. Source/model/input
and saved-proposal hashes frozen before running; mismatch stops without reuse.

Interpretation fixed in advance:

- Matrix/frame mismatch or outside-box point: do not attribute to model safety.
- Source agrees and violates property: record one original-module replay,not a
  new certification rate or change to historical result.
- Source is safe at recovered point but HZ violates: reject it as an original
  counterexample; investigate point correspondence/upstream abstraction. A
  spurious abstract point alone does NOT establish unsound overapproximation.

Six array-level acceptance controls plus existing outer controls precede freeze.
Separate saved-point/input-map and terminal audit follows. Numerical feasibility,
point correspondence and global source-to-HZ soundness remain distinct.
