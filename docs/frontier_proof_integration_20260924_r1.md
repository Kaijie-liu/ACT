# Checked-frontier real supervision integration — controls, not real results

Starting branch `feat/moe-route-verification`, clean/synced HEAD
`60aa4856dec46896bd2c812158d4aa0d2240b7b2`. New namespace `frontier_proof/` only;
the sealed source builders, frontier mechanism and older supervisors are not
edited. No dependencies installed. Existing shared-machine jobs are untouched.

## Completed

The real intake, independently accepted route evidence, lazy construction,
native retained-output proposals and fresh full-roster aggregation now run
under one hard budget. The original exhaustive arm uses the same wrapper and
original five workers; the frontier arm has seven explicit phases. New route
work is charged, not taken from a historical census or performed before timing.

Every original pair/property keeps its index. Exact strict router exclusions
discharge impossible guards; all remaining output obligations still require
fresh checked positive bounds. Construction receipts bind the checked frontier
to the new matrix. Late/partial/failed invocations cannot become complete
proofs. Batch audit retains both calls, checks actual resources and full cost,
and compares identical sources/retained matrices when both were published.
A supervisor failure is an explicit INCOMPLETE audit, not a fabricated PASS.

Control archive: `frontier_proof_controls_20260924_r1.json`.
94/94 tests PASS in 50.6893 seconds: 21 new integration/reception/batch controls
plus 73 unchanged source/frontier/arithmetic/watchdog controls. All 538 bound
implementation/protocol files match. Tests use synthetic actual checkpoints and
native LPs, plus fresh `python -S` independent audits. ZERO real model/input
decodes, real requests or real certificates. Temporary synthetic fixtures are
test artifacts, not measurements of real-model efficiency.

| Synthetic control | Original exhaustive | Checked frontier |
| --- | --- | --- |
| Three tied experts, three classes | 6/6 checked positive output bounds; complete | Same six obligations, no exclusion; complete |
| Strict router order, constant safe outputs | Two checked output bounds; four infeasible-guard LPs have no candidate; NOT_CLOSED | Four checked route-discharged duties + two positive output bounds; complete |
| Nonpositive retained output | Not used as UNSAFE | NOT_CLOSED; no safety promotion |

The second row is important: the old proposer has no independent infeasibility
certificate for an empty guard. The new mechanism can supply that missing
logical evidence, not merely run the same output LPs faster. Retained matrices
and actual declared source are identical. Real comparison must distinguish
this source of improvement from avoided propagation/construction cost.

Controls also reject changed source/run/prefix/property/direction, missing
route receipts, omitted retained properties and extra excluded properties,
forged strict exclusions and native-floating overclaims. Deadline, descendant
cleanup, exceptions, sampled RSS refusal, partial proposals, late positives,
publication overrun, missing/duplicated batch terminals and cost mutation are
covered. Request evidence checks do not import model, producer or solver code.

## Next, deliberately separate

Commit the implementation/control stage. Then freeze and independently review
exactly two requests on seed0/CIFAR4098/rank2,300s each, all252 original duties.
Use a new result directory; never overwrite sealed 98/4088/4096. The selection
is the next old manifest rank, not based on route complexity or positive bounds.
The configuration records exact source/asset/environment identities and no
automatic execution. A later explicit execution step runs the two calls once,
then saved-only audit/archive. No retry/expansion/time extension follows failure.

No experimental outcome for CIFAR4098 is reported here. A future complete
positive is a checked *declared real-graph* request, still trusting declared
graph/program correspondence, stored-center preprocessing and checker/runtime;
it is not a native floating-point execution proof, observed route flip or an
external-baseline win. See the protocol for full acceptance and stopping rules.
