# Input98: complete request evidence before floating F0 construction

Frozen repaired execution `2a8e11d76`, protocol CONV_PRE_F0_RATIONAL_R2,
same155,052-parameter convolutional checkpoint, input98, label0,2/255,
sole feasible unordered pair{1,2}. Remote publication was confirmed before
the new root was created. Independent review:
`act/pipeline/moe/results/conv_pre_f0_review_20260915_r2.json`.

## Result and trust boundary

**All9 required classification obligations are positive:** eight residual
rational McCormick LPs and one independently reconstructed scoped interval
fact. Aggregate minimum remains0.17727452738441674, supplied by the reuse
fact for competitor8. Status:
`CHECKED_REQUEST_CONDITIONAL_ON_TRUSTED_PRE_F0_LOWERING`.

The fresh router-order certificates give
`0.7053511917 <= r1-r2 <= 0.8503113761` on the supplied conditioned router HZ.
The nonnegative lower bound alone justifies the dyadic enclosure
`lambda1 in [1/2,1]` under real selected-softmax semantics. No numerical sigmoid
range or range refinement is used. Every residual has two independently
checked disagreement LP bounds on the SAME pre-F0 joint expert HZ. The builder
projects `u=q E2` and `d=q(E1-E2)` rationally on the shared factor vector and
constructs all four McCormick planes and finite corner bounds exactly.

| Competitor | Evidence | Checked lower bound (descriptive decimal) |
|---|---|---:|
|1|Rational weighted LP|5.050668686682|
|2|Rational weighted LP|1.615575029344|
|3|Rational weighted LP|2.663490004165|
|4|Rational weighted LP|3.249126116370|
|5|Rational weighted LP|4.618129704972|
|6|Rational weighted LP|2.523261849675|
|7|Rational weighted LP|4.540623214214|
|8|Scoped interval proof|0.177274527384|
|9|Rational weighted LP|6.748146445477|

Full rational values and all source/range/LP/certificate hashes are preserved
in the compact archive and raw records. The smallest residual bound is1.615575,
not the request minimum. Compared with the prior supplied-floating-F0 study,
the gate envelope is deliberately more conservative; its residual lower bounds
are lower, yet all remain positive. The objective of this study is smaller
trust, not a larger safety margin or a speed record.

The following item is **removed** from the trusted base:
`floating_F0_construction_and_scope_binding` (the weighted-property algebra and
source/property binding downstream of the supplied joint HZ are now checked).
Still trusted:

- network/input→HZ, interval-source construction and binding ordered expert
  outputs to the original model;
- membership/pair guard lowering;
- router infeasibility exclusions.

All six pair terminals are accounted for, but five exclusion bounds are not
independently proved. The checked LPs are outer relaxations; binaries are
relaxed to continuous boxes. Exact rational construction is not exact
linearization of MoE, full-network independent reproof, or deployed-float SAFE.
The conditional claim does not certify every native HiGHS/PyTorch operation.

## Execution, checks and preserved failure

R2 performs26 LP proposal calls:2 order bounds,16 disagreement bounds and8
weighted bounds. All complete; no range is refined and no property is dropped.
Two independent Python -S processes check the construction, bounds, source
scope, reused interval evidence and all9 obligations. The fresh archival
checker repeats that work without Torch/NumPy/SciPy or the rational builder.
There are no floating F0, sigmoid-range or weighted-property MILP calls in
this route. Routing and guarded propagation still use the original numerical
front-end and are not free or independently re-proved.

| Stage | Observed time | Frozen cap |
|---|---:|---:|
|Capture sources and support exports|32.38s|300s|
|Build/propose/local checks|86.49s|2100s (60s per LP)|
|First isolated checker|30.22s|600s|
|Second isolated checker|29.92s|600s|
|Total R2 stages|179.01s|Separate proof-study caps|

Fresh archival review and development tests are outside these stage times.
These are not same-budget comparative-verifier measurements. No production
gate, old checkpoint, old denominator or historical TIMEOUT was changed.
Input16 is not rerun. The model has67.06% full-test clean accuracy; this does
not close high-accuracy AdvMoE strict certification or a route-changing goal.
The only feasible pair is fixed in this observed control.

R1 under `e9999be19` is preserved as ERROR after25.67s, at its first difference
export, **before any LP proposal or new output bound**. The repair is only
canonical conversion of verified +/-1/0 property entries to Python numbers.
An early diagnosis and frozen R2 comments said "Torch scalars". Source review
corrects that wording: `linear_safety_rows` converts Torch to NumPy and returns
`numpy.float64` entries; `rational()` accepts only exact Python int/float/str
types. The original mock used Python numbers. Both NumPy and Torch scalar
conversion boundaries are now tested; the correction is separately recorded,
not written over the frozen failure archive/comments. Numerical values and
R2 mathematics are unchanged by this wording correction.

24 targeted tests pass, including actual `linear_safety_rows` NumPy types,
mocked capture with NumPy and Torch scalar inputs, complete/nonpositive request
aggregation, missing/duplicate obligations, wrong source/frame/order/property,
inward range changes, reused-fact misuse, McCormick mutations and an isolated
stdlib-only checker. R1 did not expose a failed mathematical certificate and
was not rerun to chase positive bounds. R2 uses a new frozen identity/root.

Raw roots `conv_pre_f0_rational_20260915_r1` and `_r2` remain separate under
`data/moe/results`; raw HZ/LP arrays are not committed. Prior supplied-F0 9/9
proof and production TIMEOUT remain separate, immutable results. This completes
the requested input98 proof-boundary step. No new query, tighter gate range,
input16 search or production integration is automatically queued.
