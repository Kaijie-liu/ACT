# Frozen full-request supplied-HZ evidence study R1

Inputs 16 and 98 only, original convolutional checkpoint, materialized inputs,
2/255, CPU float64 and V2 matched front-end. All nine class margins of each
fresh exact feasible pair are mandatory: {0,3}, label5; {1,2}, label0.
No historical bounds or snapshots are reused. Each of all six unordered pairs
must appear exactly once in the fresh route partition. Route exclusions and
network/guard lowering remain trusted, not independently rationally proved.

Construction-only callback replaces the monolithic execution body, preserving
its fresh common facts, scoped reuse, pair propagation, gate and difference
support and floating F0 construction recipe. It visits every property without
running any weighted-property MILP. This is a new proof-generation experiment,
NOT an identical replay or speed comparison with original 300-second requests.
Fresh source facts may discharge only their own pair/property under the
membership-domain containment rule. Each remaining supplied scalar F0 HZ is
sparsely exported, binary factors relaxed to [-1,1], and LP duals proposed.

Per request: construction subprocess cap300s, all LP proposals cap900s (60s per
LP solver), each of two isolated Python -S checks cap300s. One worker/thread,
no GPU. All attempts/costs retained. Missing generation, unavailable proposals
and nonpositive lower bounds remain UNKNOWN, never UNSAFE. Any stage error or
outer timeout stops execution and leaves unattempted cases; no retries/resume.
Every positive must exceed unchanged 1e-7. Two separate processes independently
check interval source projections/duals, exported HZ coefficients and exact
rational LP residual bounds, then complete pair/property coverage. Checker
does not call the production F0 or reuse constructor, Torch, NumPy or SciPy.

Success is CHECKED_REQUEST_CONDITIONAL_ON_TRUSTED_F0_LOWERING only if all
required obligations pass. Trusted: network/input→HZ and interval sources,
membership/pair guard lowering, router infeasibility exclusions, floating F0
construction and property binding. This is NOT the stronger pre-F0 rational
McCormick path, a MILP tree certificate, or deployed-float SAFE. Nonpositive LP
lower bounds do not imply a counterexample. Original TIMEOUTs remain intact;
production optimal-status/numerical gates are unchanged. Both expected route
sets are single-pair: no new route-changing claim.

Sources/config/jobs fixed in conv_request_sign_lp_freeze_20260915_r1.json.
Tests and commit/push precede execution. Runner requires clean feature checkout
and live remote HEAD==local HEAD BEFORE creating the result root, records the
publication receipt and checks source stability between requests. Raw root:
data/moe/results/conv_request_sign_lp_20260915_r1. No next experiment queued.
