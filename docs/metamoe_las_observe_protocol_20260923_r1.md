# Bounded old-input lAs failure observation

User authorizes separate backend repair/control, then freeze-only followup.
First collect the absent lAs key/schema evidence on MNIST7, original frozen
checkpoint, box,19global properties and strict route/nonzero adapter. One
author request only, same300s/8GiB/twoCPUthreads. Original dependency trees,
options, assertions, coefficient/bound calculations and thresholds unchanged.
The observation wrapper records nonzero counts/shapes and node used/lA flags
before invoking the ORIGINAL methods. This is not a repaired outcome or a
performance comparison; instrumentation costs are inside the one deadline.

Record all terminal states, including failure/timeout/partial events. No retry
or automatic followup. Backend repositories stay clean; wrapper is versioned
in ACT and executed through a narrowly intercepted backend launch. Independent
review validates hashes/command/terminal/cost and key mismatch, without new
solves. The old20-request execution remains sealed. Missing lAs must not be
silently zero-filled, discarded or exempted from its assertion. Only after
evidence supports a semantics-preserving compatibility repair may a separate
repair control be frozen. No new samples, time increase or ACT changes.
