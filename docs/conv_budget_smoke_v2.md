# Frozen V2 outer-supervised convolutional smoke

User-authorized bounded execution, September15,2026. This freezes a NEW smoke,
not a retry inside R1. Inputs0 and1, selected conv epoch89, epsilon2/255,
CPU/float64, E4 selected-softmax top2, all tie-legal routes. No CROWN arm.
Order is rank0 adaptive, rank0 monolithic, rank1 monolithic, rank1 adaptive,
the original rotated order with CROWN removed. Four requests maximum.

Each complete request is charged300s from before request publication and
subprocess startup, including imports/loading, routing, construction, native
solving, journaling and evidence submission. V2 reserves5s INSIDE300 equally;
25%, scoped facts, support settings and numerical gates remain unchanged.
The old owning-process-group watchdog is reused byte-identically, terminates
descendants on deadline/exception, and treats late successful packages as
TIMEOUT. Resource wait is separately reported; fixed gates16GiB RAM,5GiB disk,
load/core<=.5. One shared advisory lock, one worker,1 CPU thread, no GPU.

`scripts.conv_budget_smoke_v2` freezes protocol/source hashes, old selection,
V2 adapter identity and old-result hashes. It accepts only its single new root
`data/moe/results/conv_budget_smoke_20260915_v2`, with no resume/full options.
Clean pushed feature checkout and act-py312 required. ERROR stops the roster;
TIMEOUT/UNKNOWN remain outcomes, not reasons to retry. No checkout edits while
running. Final automatic audit is separate-process; results require a second
read-only review and archival commit/push. No automatic full experiment.

Terminal inventories bind request, progress, snapshot, journal, partial/late
packages. The journal alone cannot establish SAFE or UNSAFE. Complete accepted
packages must pass the unchanged structural auditor and full-model UNSAFE
replay, bind the exact V2 journal, and agree with WORK_COMPLETE. Both complete
and killed journals are independently checked by a Python -S interpreter;
identity is additionally checked by the terminal auditor. Open native calls
have no inferred result or exact passed limit. Incomplete last lines are only
allowed for killed/failed requests; missing journals are unavailable, not pass.

Conformance gate (frozen before execution): all4 terminals, no ERROR, valid
accounting/identity/audits, at least one complete non-error package in EACH
arm, at least one comparable equal common-fact pair, and a valid journal for
each request. No positive-count threshold. A valid audit of a failed smoke
does not make the gate pass. Zero native-overallocation in returned calls is
checked structurally; unreturned calls remain censored. Independent audit is
NOT independent numerical reproof. Even gate PASS does not authorize full90.

Compare descriptively with old same-input terminals: completion, outer cutoff,
durable property results and available native limits. Do not subtract historical
timings to invent speedup or replace R1 outcomes. Outcome need not be SAFE;
this tests whether budgeting and terminal evidence improved in actual use.

Implementation tests include real process-group cutoff, late-positive rejection,
unbound request rejection, roster/ERROR fail-stop, partial journal survival and
complete actual-toy package+journal agreement. No trained-model query before
the implementation/tests/protocol have been committed and pushed.
