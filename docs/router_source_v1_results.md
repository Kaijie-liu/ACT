# Original-parameter routing coverage composes with the real output proof

Frozen execution `766ce39f3e1ed275d0de707627f9f34cd2070f80` completed once.
This is a source-proof extension of old convolutional input98, not a new
neural verification sample, optimizer search or change to production results.
Raw and relocated artifacts: `data/moe/results/router_source_conv98_20260920_v1`.
Independent review: `docs/router_source_v1_review.json`.

## Result

All six tie-legal unordered pairs are accounted for. The five old route
exclusions now have independently checked strict source-parameter dominance
proofs on the pinned represented input box. The remaining pair is[1,2].
The checker also freshly verifies all9 original output obligations:8 rational
McCormick residuals and1 reuse fact. Minimum remains exactly
`199593373867685/1125899906842624` (about0.1772745274).

| Excluded pair | Exact proof compares | Lower bound, descriptive decimal |
|---|---|---:|
|[0,1]|outside2 minus inside0|0.0341518140|
|[0,2]|outside1 minus inside0|0.8108156204|
|[0,3]|outside1 minus inside0|0.8108156204|
|[1,3]|outside0 minus inside3|0.2232017882|
|[2,3]|outside0 minus inside3|0.2232017882|

For any legal top2 set, each selected score must be at least every unselected
score. A whole-box strictly positive outsider-minus-insider lower bound
contradicts that condition. Zero cannot exclude a legal tie. All calculations
start from raw parameter/input bytes and the declared real average-pool,
flatten and affine graph, not the original router HZ or its solver statuses.
The score1-score2 interval is approximately[0.7053511917,0.8503113761]; this
does not replace or refine the old weighted proof's gate range.

The capture recomputes and matches the frozen155,052-parameter/34-tensor model
state identity and all materialized input tensor identities. Checking matches
router bytes through that inventory and uses exact binary-rational semantics.
The producer pools input intervals; the checker independently distributes
coefficient differences over the original pixels. No solver/model library or
producer is called by the isolated mathematical checker.

## What improves, and what does not

The new status is `CHECKED_OUTPUT_REQUEST_WITH_SOURCE_ROUTER_COVER`. It removes
dependence on the five numerical infeasibility reports **for the declared real
router graph**, while retaining these explicit assumptions:

- expert network/input->HZ, interval sources and expert-output binding;
- membership/pair guard lowering;
- correspondence between the declared router graph and the registered eval
  program. Capture checks module types/attributes and parameter identities,
  but this is not a formal verification of Python/PyTorch/checkpoint decoding.

Thus this is not simply deleting one trust string: opaque exclusion claims
are replaced by checkable real-graph evidence and an explicit, narrower source
correspondence contract. Native floating-point execution and the equality of
the represented box to an intended real epsilon-ball are not proved.

The proof remains a single-pair67.06%-accuracy control. There is no new
route-changing, high-accuracy or full-network strict certificate, and no change
to0/30 convolutional production SAFE,0/20 evidence-mode positives or AdvMoE's
sealed strict pilot. The9 positive properties are not9 new requests.

## Costs and transport

| Stage | Seconds |
|---|---:|
|Parameter/input capture within builder|1.3077|
|Exact source-routing proposal within builder|0.0810|
|Copy and serialization within builder|0.0041|
|Whole build process, including startup|1.8478|
|Isolated complete-check process|30.0610|
|Total through terminal publication|31.9165|
|Separate review including second relocation/check and mutations|30.2518|

Nested costs are not summed twice. The300s budget covers this stored-source
extension only; original HZ propagation and output LP generation are excluded.
Zero new forward calls, HZ propagations or optimizer calls. No speedup claim.

The extension adds127,285 bytes; complete bundle size7,308,805 bytes. The old
bundle bytes/checker stay unchanged, with a separate hash-bound source/router
extension and composed entry point. A second copied location passes
`python -I -S`, without checkpoint, dataset, historical-run, repository or
solver reads. Its complete check takes30.0121s. Mutations that remove a route,
change a router parameter, or alter the pooling divisor are all rejected even
after their transport hashes are recomputed. Original statement/state binding
and semantic checks remain in force.

External extension identity:
`0a90d74dcf798d3a008816f0384534d6848551887a63aa211955215e12699f1b`.
Run the moved bundle's `verify_with_router.py --extension-hash <hash>` under
`python -I -S`. This is a local research artifact; no weights or bundle have
been published automatically.

Controls:6 source/capture tests and2 existing deadline/exception/partial-log
regressions PASS. A fresh independent postterminal audit returnsPASS,0issues.
Scientific next steps remain independent expert/guard lowering and genuinely
multi-route cross-family complete obligations, not further tuning of input98.
