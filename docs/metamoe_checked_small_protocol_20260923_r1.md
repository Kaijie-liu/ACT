# Small same-object author comparison: freeze only after independent smoke

The four old-input smoke PASSED; it showed the same positive set and did NOT
show ACT faster. This follow-up asks whether the combined configuration has
coverage/cost value beyond that single old positive. It is not a significance
claim, not a guarantee of winning, and not a paper-wide author reproduction.

Commit this rule and selection code before seeing new verification endpoints.
Choose **5 CIFAR10 + 5 MNIST**, separately in test index order, among first
1000 per dataset, requiring clean-correct on the same full float64 checkpoint.
Exclude every dataset/index declared in previous MetaMoE configs, including
failed attempts and old component controls. Every exclusion-source file and
the original raw data/transform/model are hash bound. No route, margin bound,
certificate, complexity, or optimizer output enters selection. Prior whole
dataset telemetry is not claimed absent. If too few qualify, STOP, no extra
scan or replacements. Author loader's fixed transforms and normalized radius
are retained; this is NOT a pixel-space epsilon claim.

Selection records every scanned index/prediction/label and materializes one
CPU/float64 box per selected input at normalized2/255, clipping[-10,10]. A
separate process replays original data/model, independently checks the first
eligible prefix, exact center/endpoints and all labels. Both selection and
review have separate300s/8GiB limits/costs and ZERO verification calls. They
are offline preparation, not silently billed as per-request solving. Failure
records remain and cannot be overwritten. Freeze is NOT execution.

Later comparison:20 complete requests, exactly same runner/auditor/options as
successful smoke, independent per-arm loads, CPU/two threads,300s/8GiB per arm,
ACT2GiB representation, unchanged internal limits, all19 properties. Rotate
ACT/author order per input, no cross-arm shared answers. Author = unchanged
native backend + route-invariance sufficient adapter. Strict route dominance
is a stronger sufficient task; failure does not refute guarded safety. ACT
HZ_POLICY_ACCEPTED and AUTHOR_BACKEND_NUMERICAL_SUFFICIENT_FILTER stay separate.

Retain positives, full-model replayed UNSAFE, completed UNKNOWN, inner backend
timeout, outer TIMEOUT, RESOURCE_LIMIT, ERROR and NOT_STARTED-after-error.
Report full charged costs of ALL attempted requests, raw statuses, paired
positive/solved intersections and each method's exclusive set. Do not infer
speed from successful requests only or interpret fast UNKNOWN as fast solving.
Native property outcomes/checked points/sign evidence stay fully auditable;
no independent SAFE reproof claim. Fixed denominators5 per dataset/10 inputs,
20 calls are not20 independent inputs. No configured success threshold or
outcome-driven retry. No parameter/model/selection modification midbatch.

This stage creates a new execution manifest/directory and stops. Historical
results and old source bindings remain sealed. Source inclusion and BN impact
review are independent of this performance comparison.
