# Frozen input98 pre-F0 rational construction study

R2 is an implementation-only repair after preserved R1 ERROR: the real
linear_safety_rows returns Torch scalar elements, whereas the mock returned
Python numbers. R1 stopped at the first disagreement export before any LP
proposal. R2 canonicalizes the checked +1/0/-1 classification template to
Python floats (exact for these integers) and adds a Torch-scalar capture test.
No input, radius, range rule, solver budget, objective, acceptance threshold,
query ordering or mathematical component changes. R1 files/root remain intact
and their failed-run inventory is bound by this new freeze. This is not a
retry/resume in the R1 directory or a response to a negative bound.

Only the same input98, label0, sole exact feasible pair{1,2}, original checkpoint,
materialized input box at2/255, CPU float64 and existing V2 common fact recipe.
No input16 query, new model, sample, production gate or historical relabel.
This is a proof-boundary follow-up, not a performance comparison.

Fresh pre-F0 capture retains ordered shared expert outputs [E1,E2] and the
conditioned router HZ. It uses common membership facts and guarded pair
propagation but forbids floating F0 construction, sigmoid-range computation
and weighted-property solving. All9 classification margins remain obligations.
Any scoped reuse is independently reconstructed from fresh source intervals.
Remaining properties receive two sparse exact projected LPs for disagreement
lower/negative-upper bounds on the SAME joint HZ. Two router LPs check score
order, giving only dyadic sigmoid envelopes with endpoints0,1/2,1. Failed order
queries retain universal endpoints; unavailable disagreement ranges leave the
corresponding property UNKNOWN. No range refinement or transcendental search.
This conservative certified gate policy is NOT the old floating sigmoid range.

The existing rational_mccormick builder reads raw stored joint-HZ coefficients
as exact binary rationals; computes u=q E2 and d=q(E1-E2) in the same factor
frame without a floating projection; introduces lambda,w; and builds all four
McCormick planes and finite corner-product bounds rationally. Binary factors
are explicitly relaxed. The separate check_rational_mccormick checker does not
import the builder and reconstructs those projections/planes independently.
The new request checker binds the range proofs to the same request, pair,
property, source and factor frame, independently checks every LP dual and scoped
fact, then aggregates all required obligations. Checked negative lower bounds
are not witnesses or proofs of negative optima.

Budgets: capture300s; all proposals/build/local checks2100s,60s per LP, at most
29 queries (2 router +2 disagreement +1 weighted per residual, at most9).
Two separate Python -S checkers each600s. One thread/worker, no GPU. All stages
and queries retained; error or timeout stops, no retry/resume. A nonpositive
output remains UNKNOWN without new queries. Fixed processing order: router
lower/upper; each ascending property's disagreement lower/upper; then ascending
weighted properties. Exact checked disagreement bounds are used verbatim.

Successful complete evidence is conditional on network/input→HZ and interval
sources/expert binding, guard lowering and route exclusions. Floating F0
construction is removed from the trusted base, NOT the whole numerical stack.
This remains a single-pair, post-selected control, not route-changing SAFE or
the high-accuracy AdvMoE objective. Original TIMEOUT and supplied-F0 positive
result remain byte-identical even if this more conservative proof is UNKNOWN.

Tests and immutable source/job freeze committed and pushed before launch.
Live remote HEAD equality is enforced before root creation. New raw root:
data/moe/results/conv_pre_f0_rational_20260915_r2. Source edits during execution
are forbidden. No next query or automatic production integration is queued.
