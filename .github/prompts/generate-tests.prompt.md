---
description: "Create integration tests for the selected code using the framework pipeline framework."
---

Generate **integration tests** for the following Python code using the pipeline testing framework.

Requirements:
- Focus on integration and end-to-end testing
- Include valid, edge, and error cases  
- Use the framework pipeline test utilities
- Mock external verifier calls when needed
- Keep tests readable and self-contained  

```python
${selection}
```
