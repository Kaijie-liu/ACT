# External real-model static-pair R2 result

Frozen execution `7e911dfab`, protocol `external_static_pair_r2.md`.
Both predetermined pairs finish on old seed0/index3000 at2/255,CPU/float64.
Same checkpoint and represented tensors; model-state identity matches the
frozen selection. No model change,retry,dtype fallback,optimization search,
dependency install or BaB. Actual imports are pinned auto_LiRPA source.

| Static pair | Positive numerical classification rows | Minimum CROWN LB |
|---|---:|---:|
|{2,4}|9/9|3.05086109|
|{4,5}|9/9|3.09949436|

All18 listed static output obligations have positive numerical lower bounds.
Forced-branch and lowered-graph outputs agree exactly on the five frozen
points per pair. These finite conformance checks bind an implementation control,
not full-domain native floating-point equivalence. Raw records include all
nine lower/upper bounds,C rows,probe margins,gate weights and graph node types.

**Task level remains static whole-box sufficient obligations.** We retained
variable selected-softmax weights and both experts,not just clean-route or
constant-weight predictions. The two-pair inventory and excluded routes come
from ACT's parent analysis. Consequently this is not an independently executed
full dynamic-MoE tool comparison, nor a direct performance competition against
HZ's guarded LP obligations. CROWN bounds are numerical filters,not an
independent outward-rounded formal proof. No high-accuracy-model claim follows.

Recorded graph/bound timings concern this tiny adapter control. Worker-body
seconds exclude interpreter/module startup; the120s external watchdog includes
startup. Total observed subprocess times were not separately retained,so no
end-to-end speedup is claimed or reconstructed from these inner timings.

`review_external_static_pair.py` checks source/adapter identities at execution
commit,external clean revisions,raw hashes,request/model/row identities,all18
counts and bound/probe consistency. PASS,zero issues. It does NOT independently
reprove CROWN bounds. Compact record:
`../results/external_static_pair_review_20260914_r2.json`.

The adapter is now demonstrated on a real trained verification-scale model,
not only the R1 toy. Wider performance and full-model equivalence remain open.
No extra pairs,samples or external settings are automatically queued.
