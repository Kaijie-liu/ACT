# Separate work after hundred-input confirmation

Planning contracts, not completed implementations or permission to silently
run new experiments. The hundred-input result is sealed and reported alongside,
not pooled with, the preceding thirty. Do not add200/500 same-family inputs,
tune25%, rerun difficult rows, or reinterpret historical composite failures.
No workstream below changes the accepted900-run results or numerical policy.

## A. Direct relationship ablation (next mechanism priority)

Question: does retaining the SAME input across expert outputs contribute
complete output proofs, holding candidate/guard/gate/property/backend/schedule
and total cost rules fixed? This is not another adaptive-vs-monolithic study.

Compare the joint set {(E_a(x),E_b(x)): x in X_S} with a sound independent
outer envelope E_a(X_S) x E_b(X_S). Preserve each expert's guarded marginal
domain, but do not accidentally identify independent factor IDs. Specify how
gate/expert dependencies are relaxed so that the full tuple still contains
every actual execution. Prove inclusion before observing effects; absence of
association is an overapproximation, never an UNSAFE witness by itself.

First deliver the inclusion argument and analytic shared/independent controls,
then freeze a small already-observed development manifest, execution order,
budget, failure rules and output metrics before model queries. Mark it as
post-confirmation mechanism follow-up, not untouched confirmation. No gate
segments, model training, tolerance, backend or scheduling changes in this
ablation. Report complete SAFE/UNSAFE/UNKNOWN/TIMEOUT and cost, not just narrower
intermediate bounds. All UNSAFE still requires concrete full-model replay.

## B. One independent external semantic-compatibility path

First pin one actual author tool revision, available license/dependencies and
configuration from primary sources. Public score tables and user-supplied
bibliography are leads, not same-task measurements. No dependency installation
or upgrade without explicit authorization. This is not a reopening of the
sealed sparse-alpha or Lagrangian parameter search.

Capability report must separate: (1) equivalent full routed weighted MoE;
(2) per-route static sufficient obligations; (3) the F0 outer relaxation.
Only(1) is a direct full-system comparison. Bind tie policy, TopK, variable
selected-softmax weights, product semantics, guarded input constraints,
preprocessing and floating-point identity. A clean-route trace or frozen
weights are not an equivalent original MoE. Finite probes alone do not prove
full-domain equivalence. Unsupported stronger obligations get their own label,
not a fabricated loss on the same task. Keep any external backend as a separate
comparator, never silently replace the ACT/HybridZ method.

## C. Request-level independently checked lower-bound evidence

The existing independent rational check covers supplied HZ -> continuous LP
relaxation -> valid lower bound. Next contract: one frozen complete request,
all required route/property obligations, explicit reused/residual partition
and aggregation. Export output properties, not only positive router margins.

Inventory every trusted boundary: network-to-HZ propagation, legal-route
exclusion, LP lowering, numerical model, and any MILP fact not independently
checked. A negative LP relaxation is UNKNOWN, not network UNSAFE. A successful
LP checker is not a MILP tree checker or proof of native floating-point code.
Do not relax optimal-status gates on the strength of solver estimates. Begin
with sparse finite-box exports and one declared request; retain all incomplete
obligations, avoiding after-the-fact selection of only positive rows.

## Later external validity, not a replacement for A--C

Freeze a different moderate-scale architecture and training/checkpoint choice
using model-quality rules before verification outcomes. Same-family repetition
is not cross-architecture evidence. AdvMoE85.67% remains an available model,
its strict pilot remains0/2; no retroactive longer timeout or smaller radius.
High-accuracy real-scale strict new certificates remain an open objective.

Each implementation/experiment needs its own new directories, tests/audit,
scope labels and commit/push. Author contact remains PI-managed. This plan
does not authorize external messages, publication or new holdout access.
